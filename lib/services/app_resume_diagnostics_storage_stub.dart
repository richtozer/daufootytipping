import 'app_resume_diagnostics_storage_contract.dart';

Future<ResumeDiagnosticsStorage> createResumeDiagnosticsStorage() {
  throw UnsupportedError(
    'Android resume diagnostics storage requires dart:io.',
  );
}
